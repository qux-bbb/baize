# git 属性：管理行尾符等（防止跨平台检出污染）
# bash 脚本永远 LF——Windows 检出保持 LF，防止 CRLF 破坏 shebang/heredoc（systemd 单元生成）
*.sh text eol=lf
# 发布仓库单一文案源永远 LF——publish_repo.sh 有 CR 行尾校验，检出须保持 LF 才不误报
server/deploy/publish/README.md        text eol=lf
server/deploy/publish/description.txt  text eol=lf
